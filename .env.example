# Database
DATABASE_URL="postgresql://school_admin:dev_password_change_in_prod@localhost:5432/school_sis?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# Session Secret (generate with: openssl rand -base64 32)
SESSION_SECRET="your-secret-key-min-32-chars-change-in-production"

# Encryption Key for PII (generate with: openssl rand -base64 32)
ENCRYPTION_KEY="your-encryption-key-32-chars-change-in-production"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# AI Features (optional)
GOOGLE_GEMINI_API_KEY="your-gemini-api-key"

# Email Provider (mock by default)
EMAIL_PROVIDER="mock"
EMAIL_FROM="noreply@schoolsis.local"

# SMS Provider (mock by default)
SMS_PROVIDER="mock"

# WhatsApp Provider (mock by default)
WHATSAPP_PROVIDER="mock"

# Payment Gateway (mock by default)
PAYMENT_PROVIDER="mock"
