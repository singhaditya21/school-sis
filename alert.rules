groups:
- name: SchoolSISAlerts
  rules:
  - alert: HighRequestLatency
    expr: school_sis_http_requests_total > 1000
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "High request volume detected"
      description: "The application is experiencing a spike in traffic (>1000 requests)."
